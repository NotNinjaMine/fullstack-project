// TODO: Replace with real unit tests for the Jordon vertical (backend/models, backend/services, backend/lib, etc.)
// Uses Node's built-in test runner: node --test tests/jordon
import { test } from "node:test";
import assert from "node:assert";

test("placeholder", () => {
  assert.ok(true);
});
