VITE_API_BASE_URL=http://localhost:3001

# Temporary: no backend yet, so src/mocks/server.js intercepts axios via
# axios-mock-adapter and serves fixture data instead. Defaults to "on" even
# without a .env file. Set to false once Members 3/4's Express API is running.
VITE_USE_MOCK_API=true
